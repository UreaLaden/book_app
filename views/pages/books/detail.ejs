<%- include('../layout/header') %>

  <body>
    <h1>
      <%= specificBook.title %>
    </h1>
    <ul>
      <li><a href="/">Home</a></li>
      <li><a href="/pages/books/show"> Results</a></li>
    </ul>
    <div>
      <div id="entry">        
        <img class='center' src="<%= specificBook.image_url %>" alt="">        
        <h2><%= specificBook.title %></h2>
        <h2><%= specificBook.authors %> </h2>
        <h4><%= specificBook.description %></h4>
        <h4><%= specificBook.isbn %></h4>
        
        <h4>Bookshelf: Genre</h4>
          <!-- idempotent: Unable to be changed -->
            <!-- http can use GET PUT POST DELETE  -->
            <!-- forms can only use GET POST -->
            <!-- we will need method-override to change it to delete -->
        </form>
        <form action="/books/<%= specificBook.id %>?_method=DELETE" method="POST">
          <button type="submit" value='delete'>Delete Book</button>
        </form>
        <form action="/books/<%= specificBook.id %>?_method=PUT" method="POST">
        <fieldset>
          <legend>Update Details</legend>
          <input type="text" name="title" value= "<%= specificBook.title %>">
          <input type="text" name="description" value= "<%= specificBook.description %>">
          <input type="text" name="author" value= "<%= specificBook.author %>">
          <input type="text" name="image_url" value= "<%= specificBook.image_url %>">
          <input type="text" name="isbn" value= "<%= specificBook.isbn %>">
          <input type="text" name="summary" value= "<%= specificBook.summary %>">
          <button type="submit" value='update'>Update Book</button>
        </fieldset>
        </form>
        
      </div>
    </div>

  </body>
  <%- include ('../layout/footer') %>